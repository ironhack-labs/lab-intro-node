class SortedList {
  constructor(items, length) {}
  add(item) {}
  get(pos) {}
  max() {}
  min() {}
  average() {}
  sum() {}
  
  let items = [];
  let length = items.length;
  
  function add(addValue) {
    return x.push(items);
  }
  
  function get(getValue) {
    return items.nth-child();
  }
  
  function maxV(maxValue) {
    if (SortedList.length === 0) {
      throw new Error("EmptySortedList")
    } else {
      return Math.max(SortedList);
    }
  }

  function minV(minValue) {
    if (SortedList.length === 0) {
      throw new Error("EmptySortedList")
    } else {
      return Math.min(SortedList);
    }
  }

  function sumV(sumValue) {
    return SortedList.reduce(value);
  }
  
  function averageV(avgValue) {
    return function sumV() / SortedList.length; 
  }

};
  module.exports = SortedList;
  